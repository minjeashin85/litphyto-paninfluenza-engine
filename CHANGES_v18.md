# 부위별 함유 비율 막대 색상을 Rank 색상과 일치

## 수정 내용
Lead Candidates Rank 카드 안의 "부위별 예상 함유 비율" 막대그래프가
항상 초록색(#059669)으로 고정되어 있었음 - Rank 배지·카드 테두리는
빨강/파랑/초록/주황/보라로 순환하는데 막대만 색이 안 맞았음.

이미 같은 스코프에 정의되어 있던 `LEAD_COLORS_TAB1` 팔레트(카드
테두리·배지에 쓰이는 것과 동일한 색)를 그대로 재사용해서 막대 색을
Rank별로 맞춤. "색이 조금 흐리면서 그라데이션으로" 요청에 따라 원색을
그대로 쓰지 않고 알파(투명도)를 섞어 부드러운 톤의 그라데이션으로
만듦 (80% 불투명 → 40% 불투명).

## 검증
- 공식 테스트 5/5 통과
- AppTest 로그인 → RUN 흐름 예외 0건
- playwright 실제 브라우저로 확인: Rank #1(빨강 배지) → 옅은 빨강
  그라데이션 막대, Rank #2(파랑 배지) → 옅은 파랑 그라데이션 막대로
  정확히 일치하는 것 스크린샷으로 확인함
