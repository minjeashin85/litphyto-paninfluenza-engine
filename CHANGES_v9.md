# AI 엔진 설정 패널 통합 박스 수정

## 원인
"AI 모델 및 LLM API 엔진 설정" 영역이 예전에 겪었던 것과 똑같은 버그
패턴이었음: `<div>`를 열어서 제목을 넣고, 그 다음 `st.columns()`로 만든
선택기/버튼들을 넣은 뒤 `</div>`로 닫으려 했는데, Streamlit 위젯은 그
HTML 문자열 안에 실제로 포함되지 않아서(별개의 컴포넌트 블록) 제목
텍스트만 초록 테두리 박스 안에 있고, "1. 연산 AI 엔진 선택" / "2. 바이오
문헌 DB 엔진 상태" / 실행 버튼은 전부 박스 밖으로 빠져나와 테두리 없이
따로 떠 있었음 (스크린샷과 정확히 일치).

## 수정
`st.container(border=True)`로 제목~선택기~실행 버튼 전체를 진짜 하나의
박스 안에 묶음. 또한 결과 영역(초록 계열)과 명확히 구분되도록 이
설정 패널은 인디고/보라 그라디언트 헤더 + 연보라 배경 톤으로 색상
테마를 다르게 줌 (API 키 발급 링크, 상태 안내박스, 라벨 텍스트 색상도
전부 통일).

## 검증
- 공식 테스트 5/5 통과
- AppTest 2개 종 전체 흐름 예외 0건
- playwright 실제 브라우저 렌더링으로 확인: 보라 그라디언트 헤더부터
  선택기, 안내 박스, 실행 버튼까지 하나의 테두리 안에 전부 들어가 있고
  초록 결과 영역과 색상으로 명확히 구분됨
