# 이번 요청 처리 결과 (특허 API 재조사 포함)

## 중요 포인트: 특허 API — 지적하신 게 맞았음, 정정함

지난 턴에 "API 키 없이 쓸 수 있는 무료 특허 검색 API가 전혀 없다"고
결론 냈는데, 이는 미국 USPTO만 조사하고 내린 성급한 결론이었음. 다시
제대로 조사한 결과:

**KIPRIS Plus(한국특허정보원)는 회원가입 후 API 키만 발급받으면 월
1,000건까지 완전 무료로 실제 특허 검색이 가능함.** 미국 USPTO의 무료
API가 2026년 6월 폐지된 것과는 별개로, 한국 특허청 데이터는 원래부터
무료 오픈 정책이었음.

- 실제 엔드포인트 확인: `getWordSearch`
  (`https://plus.kipris.or.kr/kipo-api/kipi/patUtiModInfoSearchSevice/getWordSearch`)
- `search_patents_via_kipris()` 신규 함수로 실제 연동, XML 응답 파싱
  로직을 모킹 데이터로 검증함 (제목·출원번호가 정확히 일치하는 것 확인)
- Patent Search 탭에 KIPRIS Plus + USPTO ODP 두 API 키를 나란히 입력할
  수 있게 UI 추가, 둘 다 시도해서 실제로 검색된 결과를 합쳐서 보여줌
- Google Patents는 여전히 공식 API가 없어서(스크래핑 없이는 불안정) 검색
  링크 제공으로 유지함 - 이 부분은 이전 결론이 맞았음

## 1. 박스 높이 불일치
"2. 바이오 문헌 DB 엔진 상태" 안내 박스 텍스트가 길어서 2줄로 줄바꿈되며
왼쪽 "1. 연산 AI 엔진 선택" selectbox보다 세로로 커 보였음 (스크린샷으로
확인됨) → `min-height:44px` + flex 정렬로 두 박스 높이를 정확히 맞춤.

## 2. 실행 버튼 색상
"선택된 AI 엔진으로 식물 추출 프로토콜 연산 실행" 버튼이 기본 흰색이라
안 보였음 → `st-key-run_llm_btn` CSS 클래스로 인디고/보라 그라디언트 적용.

## 3. 로그인 페이지 재수정
- 배경: 다크 → 흰색으로 변경
- ID/PW 입력 폭: 가운데 컬럼 비중을 줄여서(`[1.4, 0.9, 1.4]`) 좁게 만듦
- 로고 크기: 56px → 본 페이지 헤더와 동일한 88px 박스로 키움
- 로고 클릭 가능: 본 페이지처럼 `<a href>`로 감싸서 각 기관 사이트로 이동
  가능하게 함 (playwright로 실제 href 속성 확인함)

## 4. Download PDF 대폭 보강
- **Overview Metrics 숫자 폰트**: 15px → 11px로 축소
- **H1N1 다이어그램 결과 매핑**: 다이어그램 아래에 "본 분석 결과 기반
  표적 매핑" 표를 신규 추가 — 생애주기 7단계별로 이번 분석에서 실제로
  산출된 최상위 결합 화합물과 결합에너지를 명시함 (이미지에 직접 좌표
  오버레이하는 대신, 왜곡 없이 정확한 결과값을 표로 매핑)
- **화합물 구조 이미지 안정성 강화**: RDKit 렌더링 실패 시 PubChem 공식
  이미지로 자동 폴백하는 이중 안전장치 추가 (기존엔 실패 시 그냥
  "이미지 없음"으로 끝났음)
- **화합물별 개별 레퍼런스 신규 추가**: 기존엔 전체 통합 참고문헌
  목록 하나만 있었음 → 각 화합물 카드마다 관련 문헌 상위 3건을 개별
  표시하도록 함
- **화합물별 부위별 함유비율 신규 추가**: 화면에만 있고 PDF엔 없었던
  것을 각 화합물 카드에 추가함
- **추출법 섹션 신규 추가**: Extraction Proposals 탭 내용(옵션 3개 +
  조건 + SOP 10단계 요약)이 PDF에 전혀 없었음 → 함수 내부에서
  `generate_extraction_method_proposals()`를 직접 호출해서 항상
  포함되도록 함 ("엄격히 모든 결과를 한번에 다운로드" 요청 반영)

## 검증
- 공식 테스트 5/5 통과
- AppTest로 로그인 → RUN 전체 흐름 2개 종 예외 0건
- 실제 PDF 재생성 (7페이지로 증가) 후 페이지별 렌더링 이미지로 전부
  육안 확인: Overview Metrics 폰트 축소, H1N1 매핑표, 화합물별 부위비율/
  레퍼런스, 추출법 3개 옵션 전부 정상 출력
- KIPRIS XML 파싱 로직 모킹 데이터로 별도 검증
- playwright 실제 브라우저로 로그인 페이지(흰 배경/로고 크기/링크),
  엔진 패널 높이/버튼 색, KIPRIS+USPTO 듀얼 입력 UI 전부 스크린샷 확인
