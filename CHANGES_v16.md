# 특허 검색 - 진짜 원인 발견 및 수정

## 질문: "특허가 하나도 안 나오는게 맞아?"

**두 가지 경우를 구분해야 함:**

1. **API 키를 아예 입력 안 한 경우** → 0건 나오는 게 맞음 (의도된 엄격
   모드: 가짜 검색 링크를 특허인 척 보여주지 않기로 한 정책)

2. **API 키를 입력했는데도 0건** → 이건 진짜 버그였음, 이번에 발견해서
   수정함

## 발견한 진짜 원인

KIPRIS/USPTO 둘 다 검색 쿼리가 **"{화합물 영문명} 항바이러스"** /
**"{화합물 영문명} antiviral influenza"** 형태의 **고정된 AND 조건**
하나만 쓰고 있었음. 즉 이 단어들이 특허 문서에 **전부 정확히** 들어있어야
검색되는 구조였음.

실제로는:
- 한국 특허는 화합물명을 영문 그대로 안 쓰고 한글 음역("캠페롤" 등)을
  쓰는 경우가 많음
- "항바이러스"라는 정확한 단어 대신 "바이러스 억제", "인플루엔자 저해"
  등 다른 표현을 씀
- 미국 특허도 "antiviral"과 "influenza"라는 두 단어가 동시에 정확히
  들어있는 경우는 드묾 (화합물 자체 특허엔 이런 응용 분야 단어가 아예
  없을 수 있음)

즉 **API 연동 자체는 정상이었지만, 검색어가 지나치게 좁아서 실제
관련 특허가 존재해도 거의 항상 0건이 나올 수밖에 없는 구조적 버그**였음.

## 수정

구체적 → 일반적 순서로 여러 쿼리를 순차 시도해서, 첫 번째로 실제 결과가
나오는 쿼리를 채택하도록 변경함:

- KIPRIS: `"{화합물명} 항바이러스"` → `"{화합물명} 바이러스"` → `"{화합물명}"`
- USPTO: `"{화합물명} antiviral influenza"` → `"{화합물명} antiviral"` → `"{화합물명}"`

모킹 데이터로 검증함: 처음 두 개의 좁은 쿼리는 빈 결과를 반환하고, 화합물명
단독 쿼리에서 실제로 특허가 검색되는 시나리오를 재현해서 정상 작동 확인함.

## 검증
- 공식 테스트 5/5 통과
- AppTest 로그인 → RUN 흐름 예외 0건
- KIPRIS 쿼리 broadening 로직 모킹 데이터로 별도 검증 (좁은 쿼리 실패 →
  넓은 쿼리에서 실제 결과 도출 확인)
