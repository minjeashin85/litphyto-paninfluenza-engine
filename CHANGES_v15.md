# PDF 추출법 상세화 + 가독성 개선 + 유효물질 중심 재구성

## 1. 추출법 상세화
기존엔 SOP 10단계를 제목만 화살표로 이어붙여서("01. 채집 및 선별 → 02.
세척...") 사실상 목차 수준으로만 나왔음. 옵션 단독 PDF 다운로드와 동일한
상세도로 확장함 - 각 단계마다 번호 배지 + 제목 + 상세 설명(detail)까지
전부 표로 나열되도록 함 (Option 단독 PDF와 동일한 깊이).

## 2. 색상 박스 가독성 개선
"박스가 너무 두껍고 진하다"는 지적에 따라, 진한 원색을 꽉 채운 헤더
배너와 진한 원색 GRID 라인을 전부 걷어냄:
- **모든 섹션 헤더**: 진한 배경 풀필 → 옅은 파스텔 배경 + 좌측 3px
  액센트 바 + 원색 텍스트로 통일 (`_section_header()` 공용 헬퍼로 재사용)
- **추출법 옵션 카드**: 헤더도 옅은 배경, 테이블 GRID도 진한 원색 대신
  옅은 회색(#e2e8f0)으로, 왼쪽에만 얇은 액센트 바(2.5px)로 옵션 색상을
  표시하는 방식으로 가볍게 변경
- 옵션별 색상 구분(빨강/파랑/초록)은 그대로 유지하되 톤을 낮춤

## 3. 유효물질(핵심 결과) 중심 재구성
"유효물질들을 핵심으로 보여주게 잘 구성"이라는 요청에 따라, 표지 바로
다음에 **"핵심 유효물질 한눈에 보기" 요약 카드를 신규 추가**함 - 전체
리드 화합물을 Rank·화합물명·화학분류·결합에너지·수율 5개 컬럼으로 한
페이지에서 바로 파악할 수 있게 함 (Rank는 화면과 동일한 색상 배지로
구분). 상세 정보(구조 이미지·부위별 비율·개별 레퍼런스)는 기존처럼
Lead Candidates 섹션에서 이어짐.

## 버그 수정
작업 중 헤더 텍스트에 넣은 이모지(🎯🧬)가 PDF의 한글 CID 폰트에서 깨진
문자로 렌더링되는 걸 발견해서(예전에도 같은 문제를 겪었던 패턴) 즉시
제거함. PDF 생성 함수 전체를 정규식으로 스캔해서 남은 이모지가 없는지
재확인함.

## 검증
- 공식 테스트 5/5 통과
- AppTest 로그인 → RUN 흐름 2개 종 예외 0건
- 실제 PDF 재생성(8페이지) 후 페이지별 렌더링 이미지로 확인: 핵심
  유효물질 요약 카드, 옅은 헤더 배너 전 섹션, 추출법 10단계 상세 설명
  전부(빨강/파랑/초록 옵션 색상 유지하며 가벼운 톤으로), MOA/참고문헌
  섹션 가독성 개선 전부 육안 확인함
