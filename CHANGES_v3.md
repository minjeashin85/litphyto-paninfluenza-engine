# 이번 요청 3개 처리 결과

## 1. 탭이 안 보이는 문제 — 진짜 원인 찾음
지난 시도(`data-testid="stRadio"` 기반, 그 다음 `data-baseweb="tab"` 기반)가
왜 둘 다 안 먹혔는지 이번엔 추측하지 않고 playwright로 실제 배포 버전의
DOM을 직접 까봤음. 결과: 이 Streamlit 버전은 탭을 `<button data-baseweb="tab">`이
아니라 **`<div data-testid="stTab" role="tab">`**로 렌더링함 (BaseWeb이 아닌
React Aria 기반 구현으로 바뀌어서 `data-baseweb` 속성 자체가 없었음).

`[data-testid="stTab"]`, `[role="tab"]` 속성 선택자로 정확히 다시 잡음
(태그명 제약 없이). 실제로 계산된 스타일(`getComputedStyle`)까지 확인해서
그라디언트 배경이 진짜 적용되는 것 검증함. 5개 탭이 한 줄에 다 안 들어가고
화살표로 넘어가는 문제도 발견해서 패딩/간격을 줄여 전부 들어가게 맞춤.

크기도 15px(기존 14px)로 키우고, 활성 탭은 초록 그라디언트 필박스,
비활성 탭은 흰색 카드형으로 눈에 띄게 디자인함.

## 2. 추출 옵션 색상 구분
Lead Candidates Profiles / Patent Search 탭에서 이미 쓰던 5색 로테이션
팔레트(빨강·파랑·초록·주황·보라)를 추출법 옵션 카드에도 동일하게 적용함.
카드 테두리, Option 배지, STEP 배지, 파라미터 박스, 근거 박스까지 옵션마다
다른 색으로 통일감 있게 바뀜. 실제 스크린샷으로 Option #1=빨강, #2=파랑,
#3=초록 확인함. (PubMed/Scholar/Patent 링크 버튼 3개는 출처 종류를 나타내는
색이라 그대로 둠 - 옵션 정체성과는 다른 의미라서 안 건드림)

## 3. 다운로드 버튼 레이아웃
이모지가 텍스트 속에 떠 있던 것을, 옵션 색상 그라디언트의 둥근 아이콘
배지로 바꾸고, "Option #N 표준 공정 리포트" 제목 + "PDF 형식 · 이 옵션만
단독 저장" 부제 형태로 위계를 나눠서 정돈된 카드 스타일로 재설계함.

## 검증
- 공식 테스트 5/5 통과
- 3개 종 AppTest 전부 예외 0건
- playwright 실제 브라우저 렌더링으로 탭 크기/색상/다운로드 카드 전부
  스크린샷 확인 (5개 탭 전부 한 줄에 들어가는 것, 옵션별 다른 색인 것,
  computed style로 그라디언트 실제 적용 확인)
